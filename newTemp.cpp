#include <bits/stdc++.h>
using namespace std;
typedef long int li;
typedef long long int lli;
typedef vector<int> vi;
typedef vector<long> vli;
typedef vector<long long> vlli;
typedef set<int> si;
typedef set<long> sli;
typedef set<long long> slli;
typedef pair<int, int> pii;
typedef pair<long long, long long> pllilli;
typedef map<int, int> mii;
typedef vector<pii> vpii;

#define inpi1(n) scanf("%d", &n)
#define inpi2(n1, n2) scanf("%d%d", &n1, &n2)
#define inpi3(n1, n2, n3) scanf("%d%d%d", &n1, &n2, &n3)
#define inpi4(n1, n2, n3, n4) scanf("%d%d%d%d", &n1, &n2, &n3, &n4)
#define inpli1(n) scanf("%ld", &n)
#define inpli2(n1, n2) scanf("%ld%ld", &n1, &n2)
#define inpli3(n1, n2, n3) scanf("%ld%ld%ld", &n1, &n2, &n3)
#define inpli4(n1, n2, n3, n4) scanf("%ld%ld%ld%ld", &n1, &n2, &n3, &n4)
#define inplli1(n) scanf("%lld", &n)
#define inplli2(n1, n2) scanf("%lld%lld", &n1, &n2)
#define inplli3(n1, n2, n3) scanf("%lld%lld%lld", &n1, &n2, &n3)
#define inplli4(n1, n2, n3, n4) scanf("%lld%lld%lld%lld", &n1, &n2, &n3, &n4)
#define inpc(c) scanf("%c", &c)
#define inps(s) scanf("%s", s)
#define inpiarr(arr, n) for(int mm = 0; mm<n; mm++) scanf("%d", &arr[mm])
#define inpliarr(arr, n) for(int mm = 0; mm<n; mm++) scanf("%ld", &arr[mm])
#define inplliarr(arr, n) for(int mm = 0; mm<n; mm++) scanf("%lld", &arr[mm])
#define fi(i, a, b) for(int i = a; i<=b; i++)
#define outi1(n) printf("%d\n", n)
#define outi2(n) printf("%d %d\n", n)
#define outli1(n) printf("%ld\n", n)
#define outlli1(n) printf("%lld\n", n)
#define outs(s) printf("%s", s)
#define outiarr(arr, n) for(int mm = 0; mm<n; mm++) printf("%d ", arr[mm])
#define outliarr(arr, n) for(int mm = 0; mm<n; mm++) printf("%ld ", arr[mm])
#define outlliarr(arr, n) for(int mm = 0; mm<n; mm++) printf("%lld ", arr[mm])
#define pb push_back
#define mp make_pair
#define ft first
#define sc second

int main(){
    
    return 0;
}